function enhance(inputImage, enhancementAlgorithm) {
  return '.';
}

describe('Trench Map', () => {
  describe('enhance once', () => {
    test('simple example', () => {
      expect(enhance('.', '.')).toEqual('.')
    });
  });
});
